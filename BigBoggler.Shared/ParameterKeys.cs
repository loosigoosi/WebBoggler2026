namespace BigBoggler.Models
{
    public enum ParameterKeys
    {
        // Dati Tabellone
        Board = 1,

        // Liste Parole
        WordList = 2,
        WordListMetadata = 11,

        // Punteggi e Social
        OpponentScore = 3,
        PlayersScore = 12,

        // Tempo e Sincronizzazione
        SecondsElapsed = 10
    }
}